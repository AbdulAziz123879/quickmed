{
  "name": "quickmed-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal Express backend that proxies AI prescription-reading requests to the Anthropic API, keeping the API key server-side.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.22.0"
  }
}
